(a, b) swapWrong((b, a) t) {
	//blaat();
    return (fst(t), fst(t));

    //return must be of type (a, b) so
    //(
        // fst must be of type a
        // t must be of type (a, *) <- should not be possible
    //,
        // fst must be of type b
        // t must be of type (b, *)
    //)
}
